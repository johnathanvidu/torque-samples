spec_version: 2-preview
description: basic node webserver

grains:
  app2:
    kind: helm
    spec:
      source: 
        path: https://github.com/bitnami/charts.git//bitnami/node
      host:
        cloud-account: aws
        compute-service: eks
        #region: eu-west-1
      commands:
        - dep up bitnami/node
    
